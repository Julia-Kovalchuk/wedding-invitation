import styled from "styled-components";
import { ContainerFlexColumnCenter } from "ui/containers";

const StyledContacts = styled(ContainerFlexColumnCenter)`
  gap: 20px;
`;

export { StyledContacts };
